<%= form_with model: @donation do |f| %>
    Amount: <%= f.text_field :amount%> 
    Project: <%= f.collection_select(:project_id, Project.all, :id, :name)%> 
    User: <%= f.collection_select(:user_id, User.all, :id, :first_name)%> 
    <%= f.submit %>
<%end%> 
    